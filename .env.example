# Copie este arquivo para .env.local e preencha com os valores do seu
# projeto Supabase (Project Settings → API). Sem essas variáveis, o app
# funciona normalmente em "modo visitante" (progresso só em localStorage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
